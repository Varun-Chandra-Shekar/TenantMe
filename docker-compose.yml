services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: tenantmate
      POSTGRES_PASSWORD: tenantmate
      POSTGRES_DB: tenantmate
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U tenantmate"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
